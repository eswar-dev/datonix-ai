import type { DomainLoginTheme } from "@/domains/loginTypes";

/** Shared Datonix login brand — same across all subdomains. */
export const DATONIX_LOGIN_THEME: DomainLoginTheme = {
  pageBackground: "#152030",
  panelBackground: "#1a2a3a",
  panelHeaderBackground: "#152030",
  accent: "#0099FF",
  accentHover: "#007ACC",
  accentMuted: "#89D8F8",
  textPrimary: "rgba(255,255,255,0.92)",
  textSecondary: "rgba(255,255,255,0.65)",
  textMuted: "rgba(255,255,255,0.45)",
  border: "rgba(255,255,255,0.12)",
  inputBackground: "rgba(255,255,255,0.05)",
};
